"use strict";

// Intentionally empty.
// With `nodeIntegration: false` and `contextIsolation: true`, the renderer should have no direct access
// to Node/Electron APIs unless we explicitly expose a safe, minimal bridge.

